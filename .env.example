# Copy to .env.local for local development (gitignored).
# For production, set a GitHub Actions repository variable with the same
# name — see docs/AUTH.md.

# Google OAuth client id from https://console.cloud.google.com/apis/credentials
# (a client id is public by design; this file holds no secrets)
VITE_GOOGLE_CLIENT_ID=
